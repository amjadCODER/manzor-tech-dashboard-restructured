"use client";import {useEffect,useState} from "react";import Link from "next/link";import {select} from "@/lib/supabase-rest";export default function Tickets(){const [items,setItems]=useState<any[]>([]);useEffect(()=>{select<any[]>("tickets","select=*,profiles(full_name,email)&order=created_at.desc").then(setItems).catch(()=>{})},[]);return <main className="app-shell"><div className="topbar"><Link href="/platform">العودة</Link></div><section className="panel"><h1>تذاكر العملاء</h1><div className="ticket-list">{items.map(t=><div className="ticket" key={t.id}><strong>{t.subject}</strong><p>{t.body}</p><small>{t.profiles?.full_name} — {t.status}</small></div>)}</div></section></main>}